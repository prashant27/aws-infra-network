pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout'
                // git branch: 'main', url: 'https://github.com/prashant27/aws-infra-network.git'
            }
        }
    }
}
